# Regression Repository Context

This repository contains the first pure-methane regression candidate package.

`governance_doctrine_revision: 2`

Canonical local doctrine: `../ePC-SAFT-organization/GOVERNANCE.md`.

The package fits only methane segment count, segment diameter, and dispersion
energy through the installed provider capsule. Accepted migration receipt
`promotion-0020-regression-methane-saturation-v1` makes this repository the
production owner of that exact reproducible workflow. Independent installed-
artifact predictive and scientific admission remains validation-owned, and
the accepted receipt does not admit the fitted parameters.

Held-out pressure and density errors are descriptive only because no admission
cutoff was approved. The reporting-block directional Jacobian remains an
explicit evidence limit, with no added runtime test seam.

`runtime_source_of_truth: true`

`candidate_capability: pure-methane-saturation-parameter-candidate-v1`

`promotion_status: promotion-0020-accepted`
